box: wercker/default
build:
    steps:
        - script:
            name: install php-build
            code: sudo ./install-php-build.sh

        - script:
            name: install phpenv
            code: ./install-phpenv.sh

        - script:
            name: build PHP
            code: ./build-phps.sh
